<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Kanal.Host.ViewModels"
             xmlns:controls="using:Kanal.Host.Controls"
             xmlns:l="using:Kanal.Host.Localization"
             x:Class="Kanal.Host.Views.IconBarView" x:DataType="vm:MainViewModel">
    <StackPanel>
        <Border x:Name="Bar" BorderBrush="{StaticResource Ink}" BorderThickness="0,0,0,2"
                MinHeight="{x:Static vm:WorkspaceShellViewModel.DefaultHeaderHeight}" Padding="16,9">
            <!-- Outside the columns: a collapsed sidebar is reachable only through these. -->
            <DockPanel>
                <Button DockPanel.Dock="Left" Classes="ghost chevron" x:Name="ExpandWorkspace"
                        Margin="0,0,10,0" IsVisible="{Binding Shell.Left.CanExpand}"
                        Command="{Binding Shell.Left.ToggleCommand}"
                        ToolTip.Tip="{l:T sidebar.workspace.show.tip}"
                        AutomationProperties.Name="{l:T sidebar.workspace.show.tip}">
                    <Path Classes="glyph" Data="{StaticResource GlyphChevronRight}" />
                </Button>
                <Button DockPanel.Dock="Right" Classes="ghost chevron" x:Name="ExpandAssistant"
                        Margin="10,0,0,0" IsVisible="{Binding Shell.Right.CanExpand}"
                        Command="{Binding Shell.Right.ToggleCommand}"
                        ToolTip.Tip="{l:T sidebar.assistant.show.tip}"
                        AutomationProperties.Name="{l:T sidebar.assistant.show.tip}">
                    <Path Classes="glyph" Data="{StaticResource GlyphChevronLeft}" />
                </Button>

                <!-- A starred column gives up width, an Auto one does not: the transport must keep
                     every pixel it asked for, so the side clusters clip instead of the bar scrolling. -->
                <Grid ColumnDefinitions="*,Auto,*">
                    <StackPanel Grid.Column="0" x:Name="LeftCluster" Orientation="Horizontal"
                                Spacing="8" HorizontalAlignment="Left" VerticalAlignment="Center"
                                ClipToBounds="True">
                        <ComboBox ItemsSource="{Binding Modes}" SelectedItem="{Binding SelectedMode}"
                                  Width="160" IsEnabled="{Binding !IsRunning}" VerticalAlignment="Center"
                                  ToolTip.Tip="{Binding SelectedMode.Name}"
                                  AutomationProperties.Name="{l:T mode.tip}">
                            <ComboBox.ItemContainerTheme>
                                <ControlTheme TargetType="ComboBoxItem" x:DataType="vm:PipelineModeOption"
                                              BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                                    <Setter Property="IsEnabled" Value="{Binding IsAvailable}" />
                                </ControlTheme>
                            </ComboBox.ItemContainerTheme>
                            <ComboBox.SelectionBoxItemTemplate>
                                <DataTemplate x:DataType="vm:PipelineModeOption">
                                    <StackPanel Orientation="Horizontal" Spacing="9">
                                        <Path Classes="glyph" Data="{StaticResource GlyphChip}" Width="13" Height="13"
                                              Stretch="Uniform" VerticalAlignment="Center" />
                                        <TextBlock Text="{Binding Short}" VerticalAlignment="Center" FontSize="12.5" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.SelectionBoxItemTemplate>
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="vm:PipelineModeOption">
                                    <Grid ColumnDefinitions="Auto,*" Margin="0,3,0,4" MaxWidth="330">
                                        <Path Grid.Column="0" Width="8" Height="8" Stretch="Uniform"
                                              Margin="0,5,10,0" VerticalAlignment="Top"
                                              Data="{StaticResource GlyphSquareFilled}" Fill="{StaticResource Ink}"
                                              IsVisible="{Binding IsAvailable}" />
                                        <Path Grid.Column="0" Width="8" Height="8" Stretch="Uniform"
                                              Margin="0,5,10,0" VerticalAlignment="Top"
                                              Data="{StaticResource GlyphSquareHollow}" Fill="{StaticResource Ink3}"
                                              IsVisible="{Binding !IsAvailable}" />
                                        <StackPanel Grid.Column="1" Spacing="3">
                                            <TextBlock Text="{Binding Name}" FontSize="12.5" TextWrapping="Wrap" />
                                            <TextBlock Text="{Binding Detail}" FontSize="10.5" LineHeight="15"
                                                       TextWrapping="Wrap"
                                                       Foreground="{StaticResource Ink3}" />
                                        </StackPanel>
                                    </Grid>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <Button Classes="ghost" Content="?" Padding="9,5" FontWeight="Bold"
                                VerticalAlignment="Center"
                                ToolTip.Tip="{l:T modes.tip}"
                                AutomationProperties.Name="{l:T modes.tip}">
                            <Button.Flyout>
                                <Flyout Placement="BottomEdgeAlignedLeft">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                                  HorizontalScrollBarVisibility="Disabled">
                                        <StackPanel Margin="20,17,20,8" Width="424">
                                            <TextBlock Text="{l:T modes.heading}" Classes="label" />
                                            <Border BorderBrush="{StaticResource Ink}" BorderThickness="0,0,0,2"
                                                    Margin="0,6,0,12" />
                                            <TextBlock TextWrapping="Wrap" FontSize="12.5" LineHeight="19"
                                                       Margin="0,0,0,15" Foreground="{StaticResource Ink2}"
                                                       Text="{l:T modes.intro}" />
                                            <ItemsControl ItemsSource="{Binding Modes}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate x:DataType="vm:PipelineModeOption">
                                                        <Grid ColumnDefinitions="Auto,*" Margin="0,0,0,14">
                                                            <Path Grid.Column="0" Width="8" Height="8" Stretch="Uniform"
                                                                  Margin="0,5,11,0" VerticalAlignment="Top"
                                                                  Data="{StaticResource GlyphSquareFilled}"
                                                                  Fill="{StaticResource Ink}"
                                                                  IsVisible="{Binding IsAvailable}" />
                                                            <Path Grid.Column="0" Width="8" Height="8" Stretch="Uniform"
                                                                  Margin="0,5,11,0" VerticalAlignment="Top"
                                                                  Data="{StaticResource GlyphSquareHollow}"
                                                                  Fill="{StaticResource Ink3}"
                                                                  IsVisible="{Binding !IsAvailable}" />
                                                            <StackPanel Grid.Column="1" Spacing="3">
                                                                <TextBlock Text="{Binding Name}" FontSize="12.5"
                                                                           FontWeight="SemiBold" TextWrapping="Wrap" />
                                                                <TextBlock FontSize="10.5" LetterSpacing="0.4"
                                                                           TextWrapping="Wrap" LineHeight="15"
                                                                           Foreground="{StaticResource Ink3}">
                                                                    <Run Text="{Binding Status, Mode=OneWay}" />
                                                                    <Run Text=" · " />
                                                                    <Run Text="{Binding Mode.Leaves, Mode=OneWay}" />
                                                                </TextBlock>
                                                                <TextBlock Text="{Binding Help}" TextWrapping="Wrap"
                                                                           FontSize="11.5" LineHeight="17"
                                                                           Foreground="{StaticResource Ink2}" />
                                                            </StackPanel>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Flyout>
                            </Button.Flyout>
                        </Button>

                        <ComboBox ItemsSource="{Binding CaptureProfiles}"
                                  SelectedItem="{Binding SelectedCaptureProfile}"
                                  Width="62" IsEnabled="{Binding !IsRunning}" VerticalAlignment="Center"
                                  IsVisible="{Binding NeedsMicrophone}"
                                  ToolTip.Tip="{Binding CaptureTip}"
                                  AutomationProperties.Name="{Binding CaptureTip}">
                            <ComboBox.SelectionBoxItemTemplate>
                                <DataTemplate x:DataType="vm:CaptureProfileOption">
                                    <Path Classes="glyph" Data="{StaticResource GlyphScreen}" Width="14" Height="14"
                                          Stretch="Uniform" VerticalAlignment="Center" HorizontalAlignment="Left" />
                                </DataTemplate>
                            </ComboBox.SelectionBoxItemTemplate>
                            <ComboBox.ItemTemplate>
                                <DataTemplate x:DataType="vm:CaptureProfileOption">
                                    <StackPanel Spacing="3" MaxWidth="300" Margin="0,3,0,4">
                                        <TextBlock Text="{Binding Name}" FontSize="12.5" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding Guidance}" FontSize="10.5" LineHeight="15"
                                                   TextWrapping="Wrap" Foreground="{StaticResource Ink3}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Grid.Column="1" x:Name="Transport" Orientation="Horizontal" Spacing="4"
                                VerticalAlignment="Center" Margin="16,0">
                        <Button Classes="ghost mark" x:Name="RecordMark" Command="{Binding StartCommand}"
                                IsVisible="{Binding ShowRecord}"
                                ToolTip.Tip="{l:T transport.record.tip}"
                                AutomationProperties.Name="{l:T transport.start}">
                            <Path Classes="record" Data="{StaticResource GlyphRecord}"
                                  Width="13" Height="13" Stretch="Uniform" VerticalAlignment="Center" />
                        </Button>

                        <Button Classes="ghost mark" x:Name="PauseMark" Command="{Binding PauseCommand}"
                                IsVisible="{Binding ShowPause}"
                                ToolTip.Tip="{Binding PauseTip}"
                                AutomationProperties.Name="{Binding PauseLabel}">
                            <Panel>
                                <Path Classes="hold" Data="{StaticResource GlyphPause}" IsVisible="{Binding !IsPaused}"
                                      Width="11" Height="13" Stretch="Uniform" VerticalAlignment="Center" />
                                <Path Classes="hold" Data="{StaticResource GlyphPlay}" IsVisible="{Binding IsPaused}"
                                      Width="11" Height="13" Stretch="Uniform" VerticalAlignment="Center" />
                            </Panel>
                        </Button>

                        <Button Classes="ghost mark" x:Name="StopMark" Command="{Binding StopCommand}"
                                IsVisible="{Binding ShowStop}"
                                ToolTip.Tip="{Binding StopTip}"
                                AutomationProperties.Name="{l:T transport.stop}">
                            <Path Classes="record" Data="{StaticResource GlyphStop}"
                                  Width="12" Height="12" Stretch="Uniform" VerticalAlignment="Center" />
                        </Button>

                        <Rectangle Width="1" Height="20" Fill="{StaticResource Rule}"
                                   VerticalAlignment="Center" Margin="4,0" />

                        <Button Classes="ghost" Padding="8,6" x:Name="AudioDevices"
                                IsVisible="{Binding NeedsMicrophone}"
                                ToolTip.Tip="{l:T audio.tip}"
                                AutomationProperties.Name="{l:T audio.tip}">
                            <Path Classes="glyph" Data="{StaticResource GlyphMicrophone}"
                                  Width="10" Height="14" Stretch="Uniform" VerticalAlignment="Center" />
                            <Button.Flyout>
                                <Flyout Placement="Bottom">
                                    <StackPanel Margin="20,17,20,17" Width="292" Spacing="14">
                                        <StackPanel Spacing="6">
                                            <TextBlock Text="{l:T input.label}" Classes="label" />
                                            <ComboBox ItemsSource="{Binding Devices}" SelectedItem="{Binding SelectedDevice}"
                                                      HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate><TextBlock Text="{Binding Name}" /></DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                        <StackPanel Spacing="6" IsVisible="{Binding NeedsComputerAudio}">
                                            <TextBlock Text="{l:T output.label}" Classes="label" />
                                            <ComboBox ItemsSource="{Binding ComputerOutputs}"
                                                      SelectedItem="{Binding SelectedComputerOutput}"
                                                      HorizontalAlignment="Stretch" IsEnabled="{Binding !IsRunning}">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate><TextBlock Text="{Binding Name}" /></DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </StackPanel>
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>

                        <TextBlock x:Name="CompactState" Text="{Binding CompactState}" FontSize="11.5"
                                   LetterSpacing="0.6" Foreground="{StaticResource Ink2}"
                                   VerticalAlignment="Center" Margin="8,0,0,0"
                                   MaxWidth="300" TextTrimming="CharacterEllipsis" />
                    </StackPanel>

                    <StackPanel Grid.Column="2" x:Name="RightCluster" Orientation="Horizontal"
                                Spacing="8" HorizontalAlignment="Right" VerticalAlignment="Center"
                                ClipToBounds="True">
                        <!-- Parked here until #69 gives the meeting record its own menu. -->
                        <Button Classes="ghost" Padding="8,9" x:Name="MeetingMore"
                                ToolTip.Tip="{l:T meeting.more.tip}"
                                AutomationProperties.Name="{l:T meeting.more.tip}">
                            <Path Classes="glyph" Data="{StaticResource GlyphEllipsis}"
                                  Width="14" Height="3" Stretch="Uniform" VerticalAlignment="Center" />
                            <Button.Flyout>
                                <MenuFlyout Placement="BottomEdgeAlignedRight">
                                    <MenuItem Header="{l:T export.button}" Command="{Binding ExportMarkdownCommand}" />
                                    <MenuItem Header="{l:T export.json.button}" Command="{Binding ExportJsonCommand}" />
                                </MenuFlyout>
                            </Button.Flyout>
                        </Button>

                        <Button Classes="ghost" Padding="8,7" x:Name="JoinQr"
                                IsEnabled="{Binding CanShowJoin}"
                                ToolTip.Tip="{l:T join.tip}"
                                AutomationProperties.Name="{l:T join.tip}">
                            <Path Classes="glyph" Data="{StaticResource GlyphQr}"
                                  Width="14" Height="14" Stretch="Uniform" VerticalAlignment="Center" />
                            <Button.Flyout>
                                <Flyout Placement="BottomEdgeAlignedRight">
                                    <StackPanel Margin="20,17,20,17" Width="236">
                                        <TextBlock Text="{l:T join.label}" Classes="label" />
                                        <Border BorderBrush="{StaticResource Ink}" BorderThickness="0,0,0,2"
                                                Margin="0,6,0,12" />
                                        <StackPanel IsVisible="{Binding HasJoinInfo}">
                                            <Image Source="{Binding QrImage}" Width="196" Height="196"
                                                   HorizontalAlignment="Left"
                                                   RenderOptions.BitmapInterpolationMode="None" />
                                            <TextBox Text="{Binding JoinUrl}" IsReadOnly="True" FontSize="10"
                                                     Height="48" TextWrapping="Wrap" AcceptsReturn="True"
                                                     Margin="0,10,0,0" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding JoinError}" IsVisible="{Binding HasJoinError}"
                                                   TextWrapping="Wrap" Foreground="{StaticResource Ink2}"
                                                   FontSize="12" />
                                    </StackPanel>
                                </Flyout>
                            </Button.Flyout>
                        </Button>
                    </StackPanel>
                </Grid>
            </DockPanel>
        </Border>

        <Border IsVisible="{Binding ShowConsentGate}"
                BorderBrush="{StaticResource Rule}" BorderThickness="0,0,0,1"
                Background="{StaticResource Paper}" Padding="24,9">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="13">
                <CheckBox Grid.Column="0" IsChecked="{Binding ConsentConfirmed}"
                          Content="{l:T consent.confirm}" VerticalAlignment="Top" />
                <StackPanel Grid.Column="1" Spacing="2">
                    <TextBlock Text="{Binding ConsentReminder}" FontSize="11.5"
                               Foreground="{StaticResource Ink2}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding CaptureProfileGuidance}" FontSize="10.5"
                               Foreground="{StaticResource Ink3}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding SelectedCaptureProfile.Unavailable}" FontSize="10.5"
                               FontWeight="SemiBold" Foreground="{StaticResource Alarm}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
