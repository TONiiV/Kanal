# Copy to the repo root as .env (gitignored) for the "Host (live, .env)" launch.
# Blank values are fine: the relay simply stays disabled.
GLADIA_API_KEY=
KANAL_RELAY_URL=
KANAL_RELAY_HOST_TOKEN=
KANAL_WEB_URL=
